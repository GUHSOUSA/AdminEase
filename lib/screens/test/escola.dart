import 'package:flutter/material.dart';

class Escola extends StatelessWidget {
  const Escola({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text("Rotas para escola"),),
    );
  }
}
